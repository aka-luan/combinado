# Cloudflare Pages runs `npx @cloudflare/next-on-pages`, whose peerOptional
# workers-types@4 conflicts with wrangler@4.110+'s workers-types@5. Let npm
# install anyway so the Pages Git build can proceed.
legacy-peer-deps=true
